node_modules/ # 호스트(Windows/Mac)의 의존성 폴더가 Docker 내부로 복사되는 것을 방지합니다.
dist/ # 기존에 빌드된 로컬 결과물이 Docker 내부로 복사되는 것을 방지합니다.
.git/ # 깃 히스토리 폴더가 복사되어 이미지 용량이 커지는 것을 방지합니다.
npm-debug.log # npm 에러 로그 파일이 복사되는 것을 방지합니다.